# agent-launch.toml (your active config — replace the current one):
[roles.worker]
model = "gpt-5.5"
effort = "medium"

[roles.reviewer]
model = "gpt-5.5"
effort = "high"

[roles.orchestrator]
model = "opus"
effort = "high"

[roles.redteam]
model = "gpt-5.5"
effort = "high"
