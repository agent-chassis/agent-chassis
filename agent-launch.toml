# agent-launch.toml (your active config — replace the current one):
[roles.worker]
model = "gpt-5.6-luna"
effort = "medium"

[roles.reviewer]
model = "opus"
effort = "medium"

[roles.orchestrator]
model = "gpt-5.6-sol"
effort = "high"

[roles.redteam]
model = "opus"
effort = "medium"
