# Reasonable defaults for the Codex family. Copy to agent-launch.toml to activate.
[roles.worker]
model = "gpt-5.6-luna"
effort = "medium"

[roles.reviewer]
model = "gpt-5.6-sol"
effort = "high"

[roles.orchestrator]
model = "gpt-5.6-sol"
effort = "high"

[roles.redteam]
model = "gpt-5.6-sol"
effort = "high"
