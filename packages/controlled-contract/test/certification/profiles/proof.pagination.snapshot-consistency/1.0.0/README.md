# Snapshot-consistent pagination v1

Exact-bound proof that one declared traversal remains bound to one frozen snapshot across a relevant live-source mutation. Standalone traversal completeness is excluded.
